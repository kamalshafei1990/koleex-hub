<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Hardened-runtime entitlements for macOS code signing + notarization.
     Referenced by electron-builder (mac.entitlements). Harmless on unsigned
     local builds; required once an Apple Developer ID is configured. The JIT /
     unsigned-memory entitlements are needed by Chromium/V8; network.client is
     needed to reach the live Koleex Hub + Supabase. -->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
  </dict>
</plist>
