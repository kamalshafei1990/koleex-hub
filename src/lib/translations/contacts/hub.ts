import type { Translations } from "@/lib/i18n";

/* Contacts — the `hub.*` strings, split out of the one 225 KB dictionary.
   See the header of ../contacts.ts for why. */

export const CT_HUB: Translations = {
  "hub":                  { en: "Hub",                   zh: "中心",                 ar: "المركز" },
};
