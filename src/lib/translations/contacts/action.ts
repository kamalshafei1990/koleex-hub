import type { Translations } from "@/lib/i18n";

/* Contacts — the `action.*` strings, split out of the one 225 KB dictionary.
   See the header of ../contacts.ts for why. */

export const CT_ACTION: Translations = {
  "action.makePrimary":    { en: "Make primary",          zh: "设为主要",              ar: "تعيين كأساسي" },
};
