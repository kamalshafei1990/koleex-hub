import type { Translations } from "@/lib/i18n";

/* Contacts — the `classifications.*` strings, split out of the one 225 KB dictionary.
   See the header of ../contacts.ts for why. */

export const CT_CLASSIFICATIONS: Translations = {
  "classifications.selected":{ en: "selected",            zh: "已选",                  ar: "مُحدد" },
};
