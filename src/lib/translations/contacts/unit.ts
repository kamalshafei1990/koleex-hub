import type { Translations } from "@/lib/i18n";

/* Contacts — the `unit.*` strings, split out of the one 225 KB dictionary.
   See the header of ../contacts.ts for why. */

export const CT_UNIT: Translations = {
  "unit.days":                      { en: "days",                zh: "天",              ar: "يوم" },
};
