import type { Translations } from "@/lib/i18n";

/* Reports — the section words of the Marketing templates (`tpl.<key>.s.*`).
   No built-in marketing type yet (Phase 4E, owner, 25 Sep 2026: they come
   with the Marketing app); until then the group holds the types people make
   in the template builder, whose words travel with the type itself. */
const words: Translations = {};

export default words;
