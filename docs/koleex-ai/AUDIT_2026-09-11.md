# Koleex AI — clean-up and deep check (2026-09-11)

Owner's ask: "clean and organize the Koleex AI app, fix any problem, debug, deep-check UI/UX, and tell me what needs improving so I can decide."

Method: five independent read-only reviews over `src/components/ai`, `src/lib/voice`, `src/lib/server/ai*`, `src/app/api/ai/**`, `src/app/ai`, the docs and the validators (chat client, voice client, server security, UI/UX, repository hygiene), then every top finding verified against the code before anything was changed. Baseline `e613b38`.

Overall: no P0 on the server. Auth, the confirm ledger, attachment fencing, the picture proxy's SSRF guard and voice key handling hold as the validators claim. The defects clustered in (a) the knowledge routes, built before the AI hardening passes, (b) the legacy provider stack behind three side routes, (c) error branches of the chat client, (d) voice-call release paths, and (e) English literals inside Arabic/Chinese screens.

## Fixed in this pass (PR)

### Server — security and robustness
| # | File | What was wrong | What changed |
|---|---|---|---|
| S1 | `api/ai/knowledge/sources` GET | Listed **every tenant's** sources and unit counts to any account holding the AI Knowledge module (no `tenant_id` predicate). | Both queries scoped to the caller's tenant, same rule as `knowledge/qa`. |
| S2 | `api/ai/knowledge/sources` POST (URL ingest) | Server fetched any `http(s)` URL the owner pasted: no private-address check, redirects followed blindly, body read whole before slicing (SSRF + memory). | `assertSafeUrl` on the URL and on every redirect hop (max 3), 2 MB capped stream reader, error text no longer echoes hosts. |
| S3 | `knowledge/sources/[id]`, `knowledge/units/[id]`, `knowledge/qa` DELETE | Mutations keyed on the raw id alone. | Tenant predicate on every read/update/delete. |
| S4 | `api/ai/chat` | Full orchestrator + stream with no rate budget and no `maxDuration`; unguarded `req.json()`; user-facing text named a vendor and an env var. | Same budget block as the agent route, `maxDuration = 120`, guarded JSON, neutral message. |
| S5 | `api/ai/conversations/[id]/messages` | Same three gaps; provider error text ("<vendor> 402: Insufficient Balance") sent to the browser. | Guarded JSON, `maxDuration = 60`, vendor detail to the log, one neutral sentence to the client. |
| S6 | `api/ai/translate` | `source_lang` stored and prompted without an allow-list; unguarded JSON; no ceiling. | Allow-listed like `target_lang`, guarded JSON, `maxDuration = 60`. |
| S7 | `api/ai/product-copy` | No ceiling. | `maxDuration = 30`. |
| S8 | `lib/server/ai-provider.ts` | Six provider fetches with no deadline; Gemini key in the query string (lands in proxy/CDN logs). | 45 s `AbortSignal.timeout` on every call; key moved to the `x-goog-api-key` header. |
| S9 | `ai-agent/tools/web-search.ts` | Search answer and snippets reached the model raw — the one untrusted-content boundary not fenced. | One nonce fence (`fenceUntrusted(..., "web")`) around answer + numbered snippets; titles/URLs stay structured for citations. Validator pin added. |

### Voice call client
| # | File | What was wrong | What changed |
|---|---|---|---|
| V1 | `lib/voice/session.ts` | Hang-up during the microphone permission prompt was not honoured: the prompt resolved after `stop()`, the stream was kept by a session nobody held, the screen came back. | After the mic resolves: if the state is no longer `requesting-mic`, stop the tracks and return. `connect()`/`dialWs()` refuse an ended session. |
| V2 | `lib/voice/session.ts` | `stop()` during `setRemoteDescription` re-marked the ended call live and failed it 8 s later with a toast. | State check repeated after the await. |
| V3 | `VoiceCallButton.tsx` | A terminal failure never ran the release: wake lock held (phone never slept), call pulse left in storage (next load reported "the app was interrupted" for a failure already reported), tones/meters/streams standing. | `releaseCall()` in the terminal-failure branch. |
| V4 | `VoiceCallButton.tsx` | Unmount/reload mid-call left the wake lock and the pulse; every reload was later reported as a kill. | Cleanup releases the wake lock, timers, in-call preview and the pulse (not on `pagehide`: the pulse is what reports a page the phone kills while backgrounded). |
| V5 | `VoiceCallButton.tsx` | In-call voice preview created a new, unstoppable player per tap (overlapping samples, mic muted until they ended). | One in-call player per call over the tones' context; `stopPreview` stops it; stopped with the call. |
| V6 | `VoiceCallButton.tsx` | Wake-lock request/release race kept a lock after hang-up. | Generation counter; a lock granted after release is released at once. |
| V7 | `VoiceCallButton.tsx` | "International line unreachable" note persisted for every later call in the page's life. | Cleared on hang-up and on terminal failure. |
| V8 | `VoiceCallButton.tsx` | First mid-call error beaconed as `config-rejected` whatever its timing. | New reason `far-side-error` (telemetry allow-list + suite pin updated). |
| V9 | `VoiceCallButton.tsx` | `aria-pressed` on an action button whose name changes. | Removed; render pin updated. |
| V10 | `VoiceTranscript.tsx` | Transcript used theme tokens on the fixed black call screen — invisible under the light theme. | Fixed white / `#AAAAAA`. |

### Chat client
| # | File | What was wrong | What changed |
|---|---|---|---|
| C1 | `KoleexAiApp.tsx` | Pin/move, rename, project save/delete, first loads: no `catch` — a network throw left an optimistic pin the server never got, a danger dialog stuck open, unhandled rejections. | try/catch with rollback and a translated network error; delete dialog closes on the tap. |
| C2 | `KoleexAiApp.tsx` | Non-OK JSON envelopes discarded (a permission or rate-limit sentence became "HTTP 429"); hardcoded English "AI is not configured yet." | Envelope parsed on any status; 503 uses the translated `aiUnavailable`. |
| C3 | `KoleexAiApp.tsx`, `copy.ts`, `MicButton.tsx` | ~20 English literals inside Arabic/Chinese screens: attachment failure reasons, "Uploading", sidebar and remove-file aria-labels, the whole dictation control incl. its errors that land in the red banner. | New copy keys in all three languages; `MicButton` carries its own three-language table. Optional copy keys made required (fallbacks removed). |
| C4 | `KoleexAiApp.tsx` | Photo "remove" button was hover-only: on the phone a wrong photo could not be detached. | `[@media(hover:none)]:opacity-100`, same rule as the sidebar pin. |
| C5 | `KoleexAiApp.tsx` | Drawer footer under the iPhone home indicator. | Safe-area padding. |
| C6 | `KoleexAiApp.tsx` | Error banner had no `role="alert"`. | Added. |
| C7 | `KoleexAiApp.tsx` | `addFiles` depended on `attachments.length` while calling the copy-bearing sizer (stale language after a switch), hidden by an eslint-disable. | Depends on `addFilesSized`; directive removed. |
| C8 | `KoleexAiApp.tsx` | 42 KB emoji table in the first `/ai` chunk. | `EmojiButton` loaded on demand with a same-size placeholder. |
| C9 | `KoleexAiApp.tsx`, `ai/knowledge/page.tsx` | Dead `bottomRef`, `void persistedId`; stale comments naming vendors; "ask Claude" in knowledge-page copy. | Removed / neutral wording. |

### Hygiene
- Deleted `components/ai/AutoTranslate.tsx` (no importer; superseded by `ui/AutoTranslatedText.tsx`); manifest entry removed.
- Removed definition-only exports `hasLocalAnswer`, `localKnowledgeSize`, `DEEPSEEK_MODEL_ID`.
- Doc status lines corrected: `PHASE_15_VOICE_DESIGN.md` marked superseded by the voice roadmap; `KOLEEX_AI_EVOLUTION_PLAN.md` header no longer says DRAFT and names the flags that were never built.

## Needs the owner's decision (not changed)

### Product / UX (from the UI/UX review)
1. **One voice entry.** Merge the dictation mic into the call control (or long-press) so the composer has one "Speak"; give the in-call voice picker its own glyph; settle End (red X) vs Close (grey X) with a distinct handset glyph or an Undo toast. *M* — Done as: one control in the composer (tap = call, long press = dictate through the shared `useDictation` hook; the standalone mic stays for Discuss and the floating panel); the in-call Voice control wears the settings glyph since it opens Call settings; the sheet's Close is a chevron-down (the swipe that closes it). **End keeps the X**: the owner asked for an X, not a handset ("the end button should be X"), and that decision is recorded on the button; the spacing to the Voice control grew instead. No Undo toast: the release-on-hang-up rule frees the microphone and the socket the moment the call ends, and a 1.5 s undo would need them kept.
2. **Token pass for the AI app.** One blue (`#0066FF`) instead of three, brand functional colours, a 4-step radius scale and a 12/13/14/16/18 type ladder across `components/ai`. *M* — Done: `--kx-ai-*` tokens (accent, success, warning, danger with soft/line/text tints) declared on `.kx-ai-root`/`.kx-call-root`, `--accent` pointed at Hub Blue inside the app, every Tailwind emerald/rose/sky/amber class in `components/ai` replaced, `rounded-md` (6 px) → 8 px, the 28 px sheet corner → 24 px, and every half-pixel or sub-12 px size mapped onto 12/13/14/16/18.
3. **44 px touch-target pass** on the composer row, sidebar rows/pin/menu, bubble action row and the "Latest" chip; 16 px inputs so pinch-zoom can be re-enabled (`userScalable: false` today). *M* — Done for the AI app and the call screen (pointer-coarse hit areas, 44 px rows, 16 px inputs). The viewport lock itself is Hub-wide and stays: `globals.css` records the incident (a global 16 px rule inflated every field in every app), and lifting `userScalable: false` would make iOS zoom into every other app's 13 px inputs. Re-enabling pinch-zoom is a separate, Hub-wide decision: every app's inputs at 16 px first.
4. **Contrast pass.** Raise `--text-dim` (≈4.4:1) and the call screen's `#666666` captions (≈3.4:1) to AA; floor informational labels at 12 px. *S, visible change*
5. **URL-backed navigation** (`?c=<id>`, `?view=library|calls`) so Back works inside the PWA and chats are linkable. *M* — Done with the History API directly (no route change, no loading skeleton): user actions push, the restore on load replaces, popstate applies the address; a `?c=` in the address wins over the remembered chat, so `/ai?c=<id>` is a working link.
6. **Connection layer.** Online/offline banner, Speak disabled offline, queued resend, a proper "failed to load" card instead of the greeting on a failed chat load. *M*
7. **Voice caption redesign + Arabic register pass** (formal MSA and Egyptian colloquial are mixed on one screen). *M* — Done: the call's status is a small-caps line with a state dot (blue speaking / white listening), the transcript is 18 px white with the half-spoken line at 70 %, hints are 13 px `#AAAAAA`; the Arabic chat copy that was formal (welcome, rename, delete, regenerate, retry, empty states) now speaks the colloquial voice the rest of the app uses.
8. **Call settings sheet**: title it "Call settings" and either wire swipe-down or drop the drawn handle. *S*
9. **Focus management** for the call dialog, voice sheet, project dialog, lightbox, mobile drawer and row menus (move focus in, trap Tab, restore on close). *M* — Done: one `useFocusTrap` hook on the call screen, its Call settings sheet, the project dialog (now labelled by its title) and the lightbox; the row menu focuses its first item, walks with Arrow/Home/End and hands focus back to its trigger; the drawer is `inert` when off screen (see 10).
10. **Bigger accessibility items**: nested buttons in `role="button"` sidebar rows; hidden-but-focusable collapsed sidebar (`inert`). *M* — Done: the chat and project rows are plain containers whose title is the real button (pin and menu are siblings); the aside is `inert` + `aria-hidden` when it is off screen on a phone or collapsed on a desktop. The call screen also gained one live region outside its view layers, partial transcript lines are no longer announced, and an Escape already handled by a dialog above the call (lightbox, sheet, menu) no longer ends the call.

### Engineering
11. **Retire or support two dead-but-published routes**: `/api/ai/chat` and `/api/ai/conversations/[id]/messages` have no in-repo caller and survive only via `/api/v1/ai/*`. Retire with a 410 on the v1 path, or keep and finish hardening. Behind them sit two more provider stacks (`ai-provider.ts`, `providers/*`) that duplicate the hardened `core/transport` path — collapse once the routes are decided. *M/L* — Routes retired (legacy and v1 files removed; the v1 suite forbids a v1 file that does not re-export a legacy handler, so a 410 stub was not an option — the path answers 404). The provider stacks REMAIN for now: `api/translator` streams through `providers/deepseek.ts`, which imports `router.ts` and `providers/groq.ts`; `translate`, `product-copy`, `core/recovery`, `qa/ai` and the orchestrator's configured-check use `ai-provider.ts`. Collapsing them onto `core/transport` is the next engineering batch.
12. **Four unauthenticated lab pages in production**: `/ai-orb-lab`, `/ai-face-lab`, `/ai/orb-demo`, `/orb-lab` (no nav link, no auth, no `noindex`). Gate behind `AdminAuth` or return 404 in production; `ai-face-lab` + `KoleexRobot.tsx` (477 lines) look abandoned; `orb-demo`/`orb-lab` duplicate each other. *S*
13. **Tool-argument validation is log-only by default** (`AI_TOOL_VALIDATION=enforce` flips it). Approved by the owner; NOT flipped yet — the observation it depends on could not be made: the platform log query for `[ai.tool.validate]` lines timed out on every window wider than four hours, and the four-hour windows that did answer held no lines at all (no issues, or no tool calls — indistinguishable). Next step: set `AI_TOOL_VALIDATION=enforce` on the Preview environment first, exercise the tools there, then flip the default in code. *S*
14. **Provider error bodies logged verbatim** (can echo the user's prompt) in `providers/*`, `ai-provider.ts`, orchestrator, vision. Log status + classified reason; bodies behind `AI_DEBUG_REPLIES`. *S*
15. **Live-turn content has no route-level cap** on `/api/ai/agent` (history 48k, attachments 60k, the turn itself unbounded). Pick a ceiling. *S*
16. **Unify the error envelope** across the 31 routes (`{error}`, `{error,message}`, `{fallback,reason}`…), and stop returning Postgres/transport text on 500s. *M* — Done for every 500: `lib/server/ai/http/api-error.ts` (`dbError`, `apiError`) logs the detail under a context tag and answers `{ error: <code>, message: <one neutral sentence> }`; twenty sites in thirteen routes, plus the agent stream's error frame. The remaining shape differences (`{fallback,reason}`, `{call_id,output}`) are deliberate per-route contracts.
17. **DNS-rebinding window** in `assertSafeUrl` (resolve-then-fetch-by-name). Accept as residual or pin the connection to the vetted IP. *M* — Accepted as residual, recorded in `safe-url.ts`: the callers are the picture proxy, the voice preview and the knowledge URL ingest, all super-admin or internal-user gated, all with redirects re-checked per hop; a host that answers public-then-private within one request is a targeted attack against an authenticated insider, and pinning the connection to the vetted IP means custom TLS/SNI handling that the platform's fetch does not expose.
18. **Decompose** `send()` (~570 lines, three copies of the sidebar-bump reducer) and `startCall`/`connect`/`dialWs` (200–300 lines each) along the seams already marked in comments; add a `recoveryPlan()` owner for the four retry layers. *L* — Partly done: the sidebar-bump reducer is one function (`bumpConversation`), and edit/regenerate read the thread through refs. The larger cuts (`readAgentStream`, `uploadAttachments`, `wirePeer`/`postOffer`, `recoveryPlan`) are DEFERRED on purpose: they are behaviour-preserving refactors of the two paths the owner uses every day (the stream and the call), and the repo has no runtime harness for either — a static suite proves shape, not a live SSE stream or a WebRTC negotiation. They should be done in a session that can drive both against a real backend.
19. **Performance**: memoise `Bubble` props (inline lambdas defeat `memo`), stop meter ticks re-rendering the whole call screen (`React.memo(VoiceTranscript)`, CSS-variable path for the orb level), lazy-load `VoiceCallButton`. *M* — Done: bubble callbacks carry the message id so the parent passes one stable function per prop; `VoiceTranscript` is memoised; the call button (and `lib/voice` behind it) loads on demand. The orb still takes its level as a prop — moving it to the CSS-variable path is part of the deferred call-screen refactor.
20. **Validators run only by hand** (42 scripts, none in CI). Add an aggregate `validate:ai` and a workflow. *S*
21. **Mainland worst case**: the connecting chain can exceed two minutes (ws 15 s → rtc 50 s → grace → alt 50 s); cap total connecting time (~45 s) and say so honestly. The lane probe can also override a caller's explicit line choice on the next load — mark user-chosen lanes as such. *M* — Lane: done (`SavedLane.source` user|probe|call; a fresh user choice is not probed over). Cap: NOT applied — the slow-then-successful mainland handshakes of 2026-09-11 would have been cut off by a 45 s cap; instead the slow caption shows the elapsed seconds so the wait is a number. Revisit once the Singapore pin has a week of watchdog data.

## Verification
See the PR description for the suite, type-check, lint and build results.
